<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_ff770d</item>
        <item name="colorPrimaryDark">@color/color_ff770d</item>
        <item name="colorAccent">@color/color_ff770d</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/button_corner</item>
        <item name="android:textColor">@color/color_FFFFFF</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Button.NoBorder">
        <item name="android:background">@drawable/btn_border_selector</item>
        <item name="android:textColor">@color/color_ff770d</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ToobarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/color_FFFFFF</item>
        <item name="android:textColorPrimary">@color/color_FFFFFF</item>
        <item name="actionMenuTextColor">@color/color_FFFFFF</item>
    </style>

    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="Edit">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/color_222222</item>
        <item name="android:textColorHint">@color/color_7f6b6c70</item>

        <item name="android:background">@null</item>
        <item name="android:drawableBottom">@drawable/edit_bottom_line</item>
        <item name="android:drawablePadding">7dp</item>
    </style>

    <!--自定义dialog背景全透明无边框theme -->
    <style name="MyDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
